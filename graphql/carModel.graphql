input CreateCarModelInput {
  name: String! @rules(apply: ["required", "unique:car_models,name"])
}

input UpdateCarModelInput {
  id: ID!
  name: String
}

type CarModel {
  id: ID!
  name: String!
}